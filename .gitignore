# Delivery / build-output zips must never be committed inside the website
# folders. Source-asset archives live in /source-assets and ARE tracked.
/flash prints/**/*.zip
/flash prints/*.zip
/maskoco.com/**/*.zip
/maskoco.com/*.zip

# PHP mailer: never commit real SMTP credentials or the installed library
/flash prints/send.config.php
/flash prints/vendor/
/flash prints/composer.lock
